 -r base.txt
# Phase 1: BMKG ingestion (XML + JSON parsing, MinIO upload).
# Loaded by ingestion container in Phase 1.

requests==2.32.3
lxml==5.3.0
pydantic==2.9.2
structlog==24.4.0
minio==7.2.8
tenacity==9.0.0
